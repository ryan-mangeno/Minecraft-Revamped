#include "Atmosphere.h"

void Atmosphere::update() {
  // nothing for now , sun is static
}

void render(const Shader &shader) {
  // no atmosphere to render yet
  // sun has no rendering but does have a pos for lighting
}
